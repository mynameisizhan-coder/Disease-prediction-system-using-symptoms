# Disease-prediction-system-using-symptoms
This project implements a Disease Prediction System that accepts user-selected  symptoms as input and predicts the most probable disease using a pre-trained  Decision Tree classifier. The system is packaged as an interactive web application  built with the Streamlit framework, making it accessible to non-technical users. 
